// [property_name] = "expresion"
// (event_name) = "expression"
// => [(target)] ="expression"

// Chú ý: Đối với case two way binding khi import { FormsModule } from '@angular/forms';
// Ta nên add vào imports: [ BrowserModule,FormsModule ],